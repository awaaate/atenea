import { Toggle } from "./toggle";
export default {
  title: "Components/Toggle",
  tags: ["autodocs"],
};

const Template = (args: any) => <Toggle {...args}>Toggle me</Toggle>;

export const Default = Template.bind({});
